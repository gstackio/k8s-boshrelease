#!/bin/bash
set -eu

mkdir -p ${BOSH_INSTALL_TARGET}/bin
pushd runtime-runc
for binary in *; do
    mv -v "${binary}" "${BOSH_INSTALL_TARGET}/bin/${binary%-v*}"
done
popd
chmod +x ${BOSH_INSTALL_TARGET}/bin/*
